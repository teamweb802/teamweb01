spring:
  application:
    name: tour

  datasource:
    url: jdbc:postgresql://localhost:5432/incheon
    username: postgres
    password: 1004

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  thymeleaf:
    cache: false

server:
  port: 8184

file:
  upload-dir: uploads